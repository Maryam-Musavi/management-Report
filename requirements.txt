# پکیج‌های مورد نیاز پروژه دستیار هوشمند اوراق گام

# رابط کاربری
streamlit>=1.35.0

# هوش مصنوعی (Ollama)
ollama>=0.2.0

# پردازش داده
pandas>=2.0.0

# فقط برای build_database.py (در صورتی که فایل خام ورودی .xlsx باشد)
# پایگاه داده اصلی SQLite است و نیازی به این پکیج در اجرای عادی برنامه نیست
openpyxl>=3.1.0
